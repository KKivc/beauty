# 项目指南

本文件只记录 image_beauty 项目的 MATLAB 特有约束；通用的语言、协作、安全、规划、编辑和验证规则遵循上级 `AGENTS.md`，不在此重复。

## 项目结构

本项目使用 MATLAB 实现人脸图像美颜算法及 GUI，按职责组织：

- `src/`：美颜算法、Context、Mask 和指标计算函数。
- `gui/`：GUIDE `.fig/.m` 或 App Designer `.mlapp` 文件。
- `tests/`：MATLAB 测试脚本。
- `assets/`：示例输入和展示素材。

算法、界面回调和评价指标应尽量分离，便于单独验证。

## MATLAB 入口与运行

当前仓库没有固定脚本或工程文件。使用 MATLAB 打开项目并运行实际 GUI 入口；新增或确认入口后，在变更说明中写明入口文件。

常用入口：

- `run('main.m')`：启动 GUI（以实际入口为准）。
- `results = beautifyImage(img, params)`：独立验证算法输出。
- `results = evaluateImage(original, output, elapsed)`：计算信息熵、标准差、平均梯度和处理耗时。

## 项目级拆分与收尾约束

- 一个任务同时涉及算法、Context/Mask、缓存、GUI、测试中的两类以上时，先拆成可独立验收的子步骤；每个子步骤只设一个目标和一组验收条件，不顺带实现相邻 issue。
- “预计修改 4 个以上源文件”“需要新增 2 个以上公共入口”或“需要同时设计多个新字段/缓存元数据”时，触发范围复核并报告拆分方案。这些是复核信号，不是禁止修改的硬上限。
- 优先复用现有入口和辅助函数。每个新增公共字段、兼容 alias、缓存元数据或诊断字段都必须对应明确的消费者或测试；同一语义只保留一个规范字段，避免重复契约和无关抽象。
- 先运行受影响的目标测试，再在最终代码稳定后运行一次全量 `tests/`。只有源码、测试或环境改变，或全量测试失败后完成修复，才重新运行全量测试。
- MATLAB 大尺寸测试应在一个批处理进程中收集汇总结果；终端输出截断时读取该进程的结果，不重复启动相同测试。任务超过约 20 分钟或连续两轮返工仍未收敛时，先报告阻塞和范围变化。

## 集成验收任务执行纪律

- 验收任务默认冻结算法范围：先验证现有实现，只有定位到明确的实现根因时才修改源码；测量口径错误、测试数据前提不满足和算法回归必须分别记录，不得用放宽阈值或改写通过条件掩盖失败。
- 先建立最小验收矩阵，再扩展样本：使用一张代表性真实图跑通预览/原尺寸、零强度、缓存复用、参数组合和 GUI 主流程；确认入口稳定后，完整真实图集只运行一次，并复用同一 MATLAB 批处理进程的结果。
- 长耗时任务按“快速探针、单图定位、一次全量、最终单元测试”顺序执行。任何失败都先读取已有输出和诊断再决定下一步，不重复启动相同的完整批处理；新增或修改验收口径时必须写明依据和影响范围。
- 用户询问进度、任务超过约 20 分钟或连续两轮返工未收敛时，立即报告当前阶段、已知失败、剩余验证和预计耗时；不得在未沟通的情况下继续扩大样本、增加接口或反复调参。
- 交付时区分“代码修复”“验收脚本”“真实图观察”和“不可适用样本”，只对实际执行并通过的项目宣称完成；临时对比图和 CSV 放在临时目录，项目内只保留可复现入口和中文验收记录。

## GUI 功能契约

界面必须同屏并排显示原图和处理结果，并支持打开图像、保存图像、磨皮参数调节、美白参数调节、一键美颜和重置原图。参数变化应触发实时预览；处理耗时应按单幅图像记录并展示客观指标。

保存结果必须保留输入图像的尺寸、分辨率和画幅比例；临时缩放或多尺度计算结束后必须恢复原始输出尺寸。

## 算法约束

函数和变量采用 `camelCase`，文件名与主函数名保持一致，参数结构使用清晰字段名。

磨皮应淡化斑点、细纹并保留五官轮廓和细节；美白应自然均匀，避免偏色、假白、光晕、伪轮廓和边缘失真。不得改变人脸结构、比例或图像内容。

## 项目级验证

涉及图像链路时，验证不同尺寸、分辨率、肤色或人脸姿态的适用样本，并检查尺寸、分辨率、比例、裁剪、拉伸、变形、细节、边缘、色彩、指标和处理耗时。GUI 变更应覆盖打开、预览、调参、保存和重置路径。

## 交付说明

变更说明应列出算法或界面变化、测试图像与结果；涉及效果调整时附 GUI 截图或前后对比，并记录信息熵、标准差、平均梯度及耗时变化。不要提交生成的缓存、临时图片或个人 MATLAB 配置文件。
